import {useEffect,useMemo,useState} from 'react'; import {translations} from './translations.js'; import {preloadTileImages} from './game/gameEngine.js'; import {saveResult} from './services/sheets.js'; import LanguagePage from './components/LanguagePage.jsx'; import RegistrationPage from './components/RegistrationPage.jsx'; import StartPage from './components/StartPage.jsx'; import LoadingPage from './components/LoadingPage.jsx'; import GamePage from './components/GamePage.jsx'; import {LosePage,ClaimPage} from './components/ResultPages.jsx';
export default function App(){const [lang,setLang]=useState('en');const [page,setPage]=useState('language');const [player,setPlayer]=useState(null);const t=useMemo(()=>translations[lang],[lang]);useEffect(()=>{document.documentElement.lang=lang;document.documentElement.dir=lang==='ar'?'rtl':'ltr'},[lang]);
 const choose=l=>{setLang(l);setPage('register')}; const start=async()=>{setPage('loading');await preloadTileImages();setTimeout(()=>setPage('game'),350)}; const finish=async status=>{setPage(status==='Won'?'claim':'lose');try{await saveResult(player.number,status)}catch(e){console.error(e)}}; const again=()=>{setPlayer(null);setPage('register')};
 return <div className="app">{page==='language'&&<LanguagePage onSelect={choose}/>} {page==='register'&&<RegistrationPage t={t} lang={lang} onDone={p=>{setPlayer(p);setPage('start')}}/>}{page==='start'&&<StartPage t={t} onStart={start}/>} {page==='loading'&&<LoadingPage t={t}/>} {page==='game'&&<GamePage t={t} onFinish={finish}/>} {page==='lose'&&<LosePage t={t} onAgain={again}/>} {page==='claim'&&<ClaimPage t={t}/>}</div>}
