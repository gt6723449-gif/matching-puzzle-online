import {useState} from 'react'; import {CONTACT_URL} from '../config.js';
export function LosePage({t,onAgain}){return <main className="page center"><section className="card result-card"><div className="result-icon lose">×</div><h1>{t.gameOver}</h1><p>{t.loseText}</p><button onClick={onAgain}>{t.playAgain}</button></section></main>}
export function ClaimPage({t}){const [redirecting,setRedirecting]=useState(false);const claim=()=>{setRedirecting(true);window.open(CONTACT_URL,'_blank','noopener,noreferrer');setTimeout(()=>setRedirecting(false),1200)};return <main className="page center"><section className="card result-card"><div className="confetti">🎉</div><h1>{t.congrats}</h1><p>{t.claimText}</p><button onClick={claim} disabled={redirecting}>{redirecting?t.redirecting:t.claim}</button></section></main>}
